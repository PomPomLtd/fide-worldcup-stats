name: 🔬 Stockfish Analysis
run-name: 🔬 Stockfish ${{ inputs.round && format('Round {0}', inputs.round) || 'All Rounds' }} (depth ${{ inputs.depth || 15 }})

on:
  workflow_dispatch:
    inputs:
      round:
        description: 'Round number (1-6, or leave empty for all rounds)'
        required: false
        type: string
      depth:
        description: 'Stockfish depth (default: 15)'
        required: false
        default: '15'
        type: number

jobs:
  analyze:
    runs-on: ubuntu-latest
    permissions:
      contents: write
    timeout-minutes: 240  # 4 hours maximum for full tournament analysis

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Setup Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.11'
          cache: 'pip'

      - name: Install Stockfish
        run: |
          sudo apt-get update
          sudo apt-get install -y stockfish
          stockfish --version

      - name: Install Python dependencies
        run: |
          pip3 install -r requirements.txt

      - name: Create analysis directory
        run: |
          mkdir -p data/analysis

      - name: Download and extract PGN files
        run: |
          echo "━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━"
          echo "📥 Downloading FIDE World Cup 2025 PGN data"
          echo "━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━"
          curl -L https://worldcup2025.fide.com/files/cup2025.zip -o /tmp/cup2025.zip
          echo "📦 Extracting PGN files..."
          mkdir -p _SRC/cup2025
          unzip -q /tmp/cup2025.zip -d _SRC/cup2025/
          echo "✅ PGN files ready ($(find _SRC/cup2025 -name '*.pgn' | wc -l) files)"
          echo ""

      - name: Determine analysis scope
        id: scope
        run: |
          echo "━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━"
          echo "🔬 Stockfish Analysis Configuration"
          echo "━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━"
          if [ -z "${{ github.event.inputs.round }}" ]; then
            echo "scope=all" >> $GITHUB_OUTPUT
            echo "rounds=1 2 3 4 5 6" >> $GITHUB_OUTPUT
            echo "📊 Scope: ALL rounds (1-6)"
            echo "⏱️  Estimated time: 3-5 hours"
          else
            echo "scope=single" >> $GITHUB_OUTPUT
            echo "rounds=${{ github.event.inputs.round }}" >> $GITHUB_OUTPUT
            echo "📊 Scope: Round ${{ github.event.inputs.round }}"
            echo "⏱️  Estimated time: varies by round size"
          fi
          echo "🎯 Depth: ${{ github.event.inputs.depth }}"
          echo ""

      - name: Run Stockfish Analysis
        run: |
          echo "━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━"
          echo "🚀 Starting Stockfish Analysis"
          echo "━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━"
          echo "⏰ Start time: $(date)"
          echo "📋 Processing rounds: ${{ steps.scope.outputs.rounds }}"
          echo ""

          for round in ${{ steps.scope.outputs.rounds }}; do
            echo "━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━"
            echo "🔬 Analyzing Round $round"
            echo "━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━"

            # Find all PGN files for this round
            pgn_files=$(find _SRC/cup2025 -name "games.pgn" -path "*round${round}game*" | sort)

            if [ -z "$pgn_files" ]; then
              echo "⚠️  No PGN files found for Round $round"
              continue
            fi

            file_count=$(echo "$pgn_files" | wc -l)
            echo "📂 Found $file_count PGN file(s) for Round $round"

            # Concatenate all PGN files for this round
            echo "📥 Concatenating PGN files..."
            cat $pgn_files > "/tmp/round-${round}-all.pgn"

            # Count games
            game_count=$(grep -c '^\[Event ' "/tmp/round-${round}-all.pgn" || echo "0")
            echo "♟️  Total games: $game_count"
            echo ""

            # Run Stockfish analysis
            echo "🚀 Running Stockfish (depth ${{ github.event.inputs.depth }})..."
            echo "⏱️  This may take a while (15-30 seconds per game)..."
            echo ""
            cat "/tmp/round-${round}-all.pgn" | \
              python3 scripts/analyze-pgn.py --depth ${{ github.event.inputs.depth }} \
              > "data/analysis/round-${round}-analysis.json" 2>&1 | tee -a /tmp/analysis.log || true
            echo ""

            # Check output
            if [ -f "data/analysis/round-${round}-analysis.json" ]; then
              size=$(du -h "data/analysis/round-${round}-analysis.json" | cut -f1)
              echo "✅ Analysis complete: round-${round}-analysis.json ($size)"
            else
              echo "❌ Analysis failed for Round $round"
            fi

            echo ""
          done

          echo "━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━"
          echo "⏰ End time: $(date)"
          echo "━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━"

      - name: Verify output
        run: |
          echo "━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━"
          echo "📊 Analysis Results Summary"
          echo "━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━"
          echo ""

          if [ -d "data/analysis" ] && [ "$(ls -A data/analysis 2>/dev/null)" ]; then
            echo "📂 Generated files:"
            ls -lh data/analysis/
            echo ""

            # Show summary for each round
            echo "🎯 Games analyzed per round:"
            for file in data/analysis/round-*-analysis.json; do
              if [ -f "$file" ]; then
                round=$(basename "$file" | grep -oP 'round-\K\d+')
                games=$(cat "$file" | grep -o '"gameIndex"' | wc -l)
                size=$(du -h "$file" | cut -f1)
                echo "  • Round $round: $games games ($size)"
              fi
            done
          else
            echo "⚠️  No analysis files generated"
          fi
          echo ""

      - name: Commit and push results
        run: |
          echo "━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━"
          echo "📝 Committing Results"
          echo "━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━"

          # Check if any analysis files were generated
          if ! ls data/analysis/*.json >/dev/null 2>&1; then
            echo "⚠️  No analysis files to commit"
            exit 0
          fi

          git config user.name "GitHub Actions Bot"
          git config user.email "actions@github.com"

          git add data/analysis/*.json

          if git diff --staged --quiet; then
            echo "ℹ️  No changes to commit (files unchanged)"
          else
            if [ "${{ steps.scope.outputs.scope }}" = "all" ]; then
              git commit -m "$(cat <<'EOF'
          Add Stockfish analysis for all rounds

          Generated by GitHub Actions
          EOF
          )"
            else
              git commit -m "$(cat <<'EOF'
          Add Stockfish analysis for Round ${{ github.event.inputs.round }}

          Analyzed with depth ${{ github.event.inputs.depth }}
          Generated by GitHub Actions
          EOF
          )"
            fi
            git pull --rebase
            git push
          fi

      - name: Summary
        if: always()
        run: |
          echo "━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━"
          echo "📊 Analysis Summary"
          echo "━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━"
          if [ "${{ steps.scope.outputs.scope }}" = "all" ]; then
            echo "Scope: All available rounds"
          else
            echo "Scope: Round ${{ github.event.inputs.round }}"
          fi
          echo "Depth: ${{ github.event.inputs.depth }}"
          echo ""

          if [ -d "data/analysis" ]; then
            file_count=$(ls -1 data/analysis/round-*-analysis.json 2>/dev/null | wc -l)
            echo "Files generated: $file_count"

            total_size=$(du -sh data/analysis 2>/dev/null | cut -f1 || echo "0")
            echo "Total size: $total_size"
          else
            echo "No analysis files generated"
          fi
